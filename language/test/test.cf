main :: () -> s32 {
  add_two_to_two()
}

add_two_to_two :: () -> s32 {
  2 + 2
}

/*

main :: () {
  fib_log(15)
}

fib_log :: (n: u32) {
  if n == 0 {
    log(fib(0))
  } else {
    fib_log(n - 1);
    log(fib(n));
  }
}

fib :: (n: u32) -> u32 {
  if n < 2 {
    n
  } else {
    fib(n - 1) + fib(n - 2)
  }
}
*/
